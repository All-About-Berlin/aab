[project]
name = "aab-monitor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "requests",
    "feedparser",
    "beautifulsoup4",
    "lxml",
    "platformdirs",
    "playwright",
]
